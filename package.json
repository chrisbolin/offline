{
  "name": "offline",
  "version": "0.2.0",
  "private": true,
  "dependencies": {
    "react": "^15.6.1",
    "react-detect-offline": "^1.0.2",
    "react-dom": "^15.6.1",
    "react-scripts": "1.0.10",
    "serve": "^6.0.5",
    "styled-components": "^2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "react-scripts build && rm -rf docs && cp -r build docs",
    "serve": "serve -s docs",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "homepage": "."
}
